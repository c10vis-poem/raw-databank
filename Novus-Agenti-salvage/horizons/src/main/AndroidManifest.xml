<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- Agent: device control -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <!-- Agent: PIM (runtime — requested in PermissionsSetup) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Agent: app inventory -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Agent: media session access (granted via NLS binding) -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <!-- Agent: Do Not Disturb policy -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- NPU performance: Game SDK scheduler boost -->
    <uses-permission android:name="android.permission.HIGH_PERFORMANCE" />

    <uses-feature android:name="android.hardware.game" android:required="true" />

    <queries>
        <package android:name="com.termux" />
    </queries>

    <application
        android:name=".HorizonsApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Horizons"
        android:allowBackup="false"
        android:appCategory="game"
        android:isGame="true"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Horizons">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Local UI fork (session 16) — additive, does not replace MainActivity.
             Separate launcher icon so both UIs are installable side by side while
             this fork is under active development. -->
        <activity
            android:name=".uilocal.LocalHomeActivity"
            android:exported="true"
            android:label="Novus Agenti (Local)"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Horizons">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".tiles.TerminalTile"
            android:icon="@android:drawable/ic_menu_manage"
            android:label="Horizons Terminal"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".accessibility.HorizonsAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
          ASSIST API (Voice Interaction) — ACTIVE.
          To take effect on device: Settings → Apps → Default apps →
          Digital assistant app → pick Horizons. After that, long-press home
          (or the assist gesture) hands us the foreground screenshot + view
          tree via HorizonsVoiceInteractionSession — no MediaProjection
          consent prompt. Horizons replaces Google Assistant as the system
          assistant while selected.
        -->
        <service
            android:name=".assist.HorizonsVoiceInteractionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <service
            android:name=".assist.HorizonsVoiceInteractionSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true" />

        <service
            android:name=".assist.HorizonsRecognitionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
        </service>

        <!-- Agent: NotificationListenerService — grants read access to notification shade.
             User must enable: Settings → Notification access → Horizons. -->
        <service
            android:name=".core.agent.AgentNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- CLIFFORD (BRD) — daemon launcher + CRS recovery loop, isolated process -->
        <service
            android:name=".fgs.CliffordService"
            android:foregroundServiceType="specialUse"
            android:process=":clifford"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="NPU daemon guardian" />
        </service>

        <!-- Restart CLIFFORD after device boot (deferred 30s for Android 15+ FGS restriction) -->
        <receiver
            android:name=".fgs.CliffordBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- G14: Mode B — Live Chat FGS (audio loop + LLM stream, no screen) -->
        <service
            android:name=".fgs.LiveChatService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- G14: Mode A — Live Screen Share FGS (MediaProjection + audio loop + LLM stream) -->
        <service
            android:name=".fgs.ScreenShareService"
            android:foregroundServiceType="mediaProjection|microphone"
            android:exported="false" />

        <!-- System TTS engine — Kokoro on-device via Sherpa-ONNX.
             Settings → Accessibility → Text-to-speech → Preferred engine → Horizons.
             Any app calling TextToSpeech.speak() routes through here. -->
        <service
            android:name=".core.voice.HorizonsTtsService"
            android:label="Horizons TTS (Kokoro)"
            android:permission="android.permission.BIND_TEXT_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TTS_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_engine" />
        </service>

        <!-- Model file handler — "Open with Horizons" from browser/file manager.
             Covers .bin/.serialized.bin (QNN context binary), .onnx, .gguf (llama.cpp),
             .tflite, .dlc (SNPE), .pte (ExecuTorch), .qnn downloads. -->
        <activity
            android:name=".ModelImportActivity"
            android:exported="true"
            android:theme="@style/Theme.Horizons"
            android:label="Import to Horizons">

            <!-- Handle file:// and content:// URIs with model extensions -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <!-- Handle file open from file managers -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.bin" />
                <data android:pathPattern=".*\\.onnx" />
                <data android:pathPattern=".*\\.gguf" />
                <data android:pathPattern=".*\\.tflite" />
                <data android:pathPattern=".*\\.dlc" />
                <data android:pathPattern=".*\\.pte" />
                <data android:pathPattern=".*\\.qnn" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Handle SEND intents (share model file to Horizons) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

    </application>
</manifest>
