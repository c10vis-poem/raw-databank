# Tools

Scripts this repo uses. Empty until the grill session says what's needed.

Deterministic scripts only — anything that reads a source and writes a
derivative must be re-runnable and produce the same output.
