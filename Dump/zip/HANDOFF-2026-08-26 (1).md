# Handoff — 2026-08-26

Supersedes `HANDOFF-2026-08-24.md`. Where they disagree, this file wins.

## Read first

**`README.md` → "What the grill session has to produce"** — the operator's
mandate, items 1–8 plus the wiring list. That is the load-bearing document.

## Renamed today

|  | was | now |
|---|---|---|
| Repo | `github.com/c10vis-poem/novae-corpus` | `github.com/c10vis-poem/novae-xorpus` |
| Local | `…/OBSIDIAN_VAULT/novae-corpus` | `…/OBSIDIAN_VAULT/novae-xorpus` |
| Display | NovÆcorpus · NovÆcopia | **NovÆxorpus · NovÆxopia** |

The `x` carries the hard `k` sound; the vault repo keeps its hyphen
deliberately. See `NAMING-CANON.md` → "Why `x`, and why the one hyphen".
`HANDOFF-2026-08-24.md` still names the old path and is kept as history.

**NovÆxorpus is the vault** — a replacement, not a sibling. Three legacy
Obsidian vaults sit beside it; `OBSIDIAN_VAULT.md/` and `OBSIDIAN-WIKI.md/`
are near-duplicates of each other (same files, same subpaths, both 340 `.md`),
so the 967 MB across them is roughly 480 MB of real content. `.OBSIDIAN/` is an
empty shell, 0 files, safe to delete. Resolved by grill steps 1–2.

## State

```
01-sources/   93 sources, 14 directories (+ 5 MANIFEST* files; 98 raw)
02-clean/     92 files
03-check/     EMPTY — the RLVR pass has never run
```

`tools/clean.py` converts the whole tree in one pass. Re-run it after any
retrieval; it is idempotent. Its loss check does **not** satisfy hard rule 5 —
same script, no vote. 78 zero-loss, 14 furniture-only, 0 unexplained, 1 skip
(`technical-builder-style.skill.zip`, a zip has no text).

## Two claims from the last handoff that were wrong

**rclone was never needed.** No remote is configured and none is required. The
Drive connector works. When a result exceeds the inline cap the harness spools
it to a JSON file on disk and returns the path — read the file, decode
`content`, write bytes, and the base64 never enters context. Proven on a
1.88 MB PDF. **Never paste base64 through model output**; it truncates silently
and corrupted a file once.

**"Nothing else is blocked on it" was false.** Retrieval, disambiguation and
cleaning were all available the whole time.

## Ambiguities closed

- **Item 3 · Continual Harness** — six copies exist, not five. The two `.txt`
  copies differ by 3 bytes (a BOM); all text copies are the same document. The
  real source is the **arXiv paper**, `2605.09998v1`, 31,751,676 bytes, 28
  pages — it was already on this device in three identical copies and is now in
  `01-sources/continual-harness/`.
- **Item 12 · `•⛔-whyyoucodevoicelikeass-`** — **exists**, id
  `1pQujc0sNgy45-xJsHQb2Fb9R64UduuPN`, inside `--🎯 PENDING _❓❓⏳--`. The old
  "NOT FOUND" entry was wrong. 9 files, 2 PDFs, all retrieved.
- **Item 13 · Recursive Training** — the two PDFs share an identical Drive
  title but are different documents: 1,878,046 / 12pp and 1,689,751 / 11pp.
  Both retrieved. The pick is still the operator's.

Still open: **item 7**, "all four of these PDFs" at 12:10.

**`ReadMe*` folder re-checked directly — the old tree map was stale.** The
`## Part 1- Lex (1).txt` needed for `part1-lex` is not at "My Drive root" as
the source list says; it's three folders deep, `ReadMe*/.ReadMe/Needs Edit/`.
`.ReadMe` and `Needs Edit` were both created 2026-08-26, same day as this
session — someone's been reorganizing. Also found, undocumented in the old
tree: a Doc titled `Copy of NovÆcopia~` (a project manifesto, distinct from
the existing `NovÆcopia Vincet` source), plus three loose files sitting
directly in `ReadMe*` the old map never listed. Retrieved the Lex file and
the manifesto as text extracts (not real originals — same caveat as the
`Universal_Memory_` stand-ins); the Lex one is truncated. The three other
loose files in `ReadMe*` were not checked further this session.

## Source defects found, not repairable here

- `local voice layer ` is **truncated in Drive itself** — 3,239 bytes matching
  Drive's own figure, ending mid-word at `"will show a sy"`.
- `whyyounocodegoodai` and `in order to bypass the memory killer…` are
  **byte-identical**: two Docs, different IDs and titles, same 6,035-byte
  export. Both kept, nothing merged.
- `nova-corpus-device-stack.md` — the word-frequency loss check **does not
  work** on it. Fused tokens (`CHIPSM8750`) hide values from a word diff.
  Recorded in its frontmatter; treat as unverified until 03-check runs.

## Retrieval gaps — pick up here

| Directory | Have | Need |
|---|---|---|
| `part1-lex` | **1 (text extract, truncated)** | real docx-or-txt original of `## Part 1- Lex (1).txt`, + ~2 Doc dups |
| `Coding-questions` | 0 | 2 Docs, parent `1ICuqa9Qe2cLsxKbRoZp2M_XVXJR0ZRXK` |
| `TERMUX` | 1 | 2 Docs + `DroidDesk` PDF (365,445) |
| `App_Builders_Guide_` | 1 | ~3, parent `1ra_KIcV5isSTwya39d06hBt0PDtuwymT` |
| `AESOP_XI_` | 7 | ~1, parent `1jps9u-uFBW7-ZXJkFu_aH8cZkf9TFX7p` |
| `Universal_Memory_` | 0 | **UNBLOCKED, enumerated** — 11 new files, see below |

`Universal_Memory_` — **resolved 2026-08-26.** Run from a fresh session, exactly
as this handoff instructed, and it enumerated with no refusal. The advice was
right: 11 files + 3 subfolders (`TRAINING `, `PROTOCOLS`, `KV_Cache_Compression`
— the last is empty). 3 of the 14 entries are already in the corpus: the
`XContinual_Harnessl.txt` BOM twin, the Continual-harness Doc, and the
1,689,751-byte Recursive Training PDF (exact byte match). Full table with ids
and sizes in `SOURCE-RETRIEVAL-MAP.md` → "Item 16 · `Universal_Memory_`".

**Checked device-wide before treating anything as new** — operator's instinct
was right that some of these might already be sitting around. One was:
`both.docx` turned out byte-identical (sha256 match) to a copy already on
device in two legacy vaults. Copied straight across, no Drive download,
verified by hash, cleaned. Now in `01-sources/universal-memory/`.

Two more were checked by actually reading the text, not just comparing sizes
(operator caught that the first pass hadn't). `URL's.docx` (10,500B) +
`MEMORY LAYER .docx` (19,439B) turned out to be **fragments** of Drive's
`URL's-MEMORY LAYER .docx` (20,651B), not separate drafts — the Drive file
contains both on-device files verbatim plus more chat content found nowhere
on device. Still needs full retrieval. And `1. Well I need to plan...` — the
on-device `.md` is confirmed word-for-word identical to the Drive Doc by
content, not just title; still not used, since it's a conversion and not a
raw original (no docx/original twin exists on device), so it still fails
hard rule 1 — but that call is now verified, not assumed.

**Resolved 2026-08-26.** The operator downloaded 9 of the 10 remaining files
straight from Drive to the device (`/storage/emulated/0/Download/`), which
sidesteps the API base64 problem entirely — no model-output round-trip, no
corruption risk. All 9 verified byte-exact against Drive's reported size,
copied into `01-sources/universal-memory/`, cleaned:
`URL's-MEMORY LAYER .docx`, `agent_panel.sh.docx`,
`Mapping Runtimes and CLIs -!DOCTYPE.txt`, and PDF exports of the five native
Docs (`Starting Point`, `The Architecture: How Data Moves`,
`Setting up a local shared memory layer`,
`1. Well I need to plan all this shit for later This...`,
`Yeah, you're familiar with the open source memory layer...The MemO repo`)
plus `TRAINING/failure_log_template.md.pdf`.

Before the download landed, `read_file_content` (not `download_file_content`)
was tested across every file type in this set — native Docs, an uploaded
docx, and a plain txt — and all returned real text with no base64 involved.
Two of those extracts cut off mid-sentence (`agent_panel.sh`, the MemO-repo
one), so they were only ever stand-ins, now superseded by the real downloads.

**One file remains a stand-in, not a real original:**
`TRAINING/Page - Purpose - Key evidence sources` — the operator's download
batch didn't include it. It's still the `read_file_content` text extract,
and it visibly cuts off mid-sentence ("...4 act"). Needs a manual download
like the other 9, or stays flagged for `03-check/` as known-incomplete.

Four agents died mid-run today: two on `Connection lost mid-response`, two on
session limits. Agent work must be re-checked on disk, never trusted from the
report — though every report that did arrive was honest, including one that
self-reported truncating a file and re-pulled it.

## Security — process fixed, tokens NOT rotated, by operator's choice

The operator declined rotation and asked for the process to stop producing
leaks. Done:

- **`url.insteadOf` installed globally** — any `https://github.com/…` URL is
  rewritten to SSH before git stores it, so a pasted token cannot land in a
  `.git/config`. Confirmed working live on `~/repos/obsidian-skills`.
- **`~/.git` deleted.** Home is no longer a git repo. It held 122,018 orphaned
  blobs / 2.8 GB from a mass `git add` on 2026-08-18 00:53 that never
  committed. 5.3 GB reclaimed. Nothing was ever committed, so nothing was ever
  pushed — that data never left the device.
- The `ghp_PZUS…` token named in the NovA-Corpus Diagnostic is **gone** from
  `~/repos/obsidian-skills/.git/config`.

Still live, operator's call: the `github_pat_11CAHMQQY0…` PAT, and the keys in
`~/.config/watch/.env` plus the GCP service account.

**Not installed:** a pre-commit guard blocking commits that contain token or
key patterns. Proposed via global `core.hooksPath`; caveat is that a global
hooks path overrides per-repo hooks.

## The mandate grew today — read the README first

Three new sections were added to `README.md` → "What the grill session has to
produce", all operator-stated 2026-08-26. They are not optional colour; they
change what the grill session is aiming at.

- **"Tools, not just skills"** — corrects a real bias in items 1–3. Most
  source documents describe parameters, guidelines and pathways, not actions.
  Expect **at least as many tools extracted as skills, probably more.** A plan
  that produces only a skill library has misread the corpus.
- **"The shape of the output"** — the grill session's deliverable now has a
  format template: the operator's hand-annotated architecture document at
  `01-sources/architecture-edits/`. Copy its *structure* (numbered file blocks,
  explicit destination paths, ASCII topology, inline JSON schemas), not its
  content — the content predates the three-APK architecture. Includes a
  12-row punch list built from the operator's own annotations.
- **"Tool and asset assignment"** — ECC, Honey for Devs and Pocock Skills are
  the critical three, ahead of everything. Everything else is assigned
  **per-agent**, and much of it is explicitly trial-and-error.

**Both open items from that section are now closed.** The open-source
Firecrawl equivalent is **Crawl4AI**, confirmed by the operator. And the
sentence that cut off mid-thought in the annotated document continues:
Prime Agent, code review graph, Pocock skills, reverse skills, Obsidian
skills, notebooklm-py, Graphify skills, OpenAI, and more — all now in the
README's working inventory. The load-bearing point: **those repos and skill
packages are themselves source material**, extracted for tools and skills the
same way a vendor SDK manual is, not merely dependencies to install.

**Operator decision recorded:** where `NovÆcopia Vincet` and
`Copy of NovÆcopia~` disagree, the `Copy of` wins — it's the upgraded version
with strictly more information. Both stay in the corpus. Details in
`SOURCE-RETRIEVAL-MAP.md`.

## Next

1. `03-check/` — the RLVR pass. Nothing has been checked by an independent
   tool. This is the largest outstanding gap.
2. Close the six retrieval gaps above.
3. Item 7 needs the operator.
4. Wiring named but not done: Claude-in-Obsidian, Obsidian ↔ GitHub, graphify
   on the main account, notebooklm-py.
5. Route the working inventory (README → "Tool and asset assignment") through
   the corpus process — those repos and skill packages are source material,
   not just dependencies.

## Working rules learned today

- **Do not stop to ask permission to continue** agreed work. Execute, report.
- **Keep replies short.** Lead with the result.
- **Sources keep their original spellings** — Drive titles like
  `NovÆcopia Vincet` are never rewritten to canon.
- **Search by byte size, not filename.** Three files were found on device that
  a name search missed entirely, one under a totally unrelated name and one
  with no extension at all.
