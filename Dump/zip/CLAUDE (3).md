# NovÆxorpus — Claude Code pointer

This repo's real rules are tool-agnostic and live in **`AGENTS.md`** — read that,
not this. This file exists only so Claude Code's own auto-load convention has
something to find; any other agent/tool reads `AGENTS.md` directly.
